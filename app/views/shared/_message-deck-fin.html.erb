<% if @deck.user == current_user %>
  <div class="container d-flex flex-column align-items-center hidden-text">
  <h2 class="text-center fw-bold"> BRAVO ! 🥳</h2>
  <p class="text-center fw-bold"> Vous avez terminé votre vote, partagez le lien suivant à vos amis
  pour qu’ils puissent voter à leur tour :
  <br>
  <div data-controller="clip-board" data-clip-board-feedback-text-value="Copied!">
  <input class="d-some" data-clip-board-target="input" type="text" value=" <%= deck_url(@deck) %>" readonly>
  <button class="btn btn-primary" data-action="click->clip-board#copy">Copier le lien</button>
</div>
   </p>

  <p class="text-center fw-bold"> Vous êtes le propriétaire de ce sondage, vous pourrez le clore depuis le dashboard!</p>
  </div>
<% else %>
  <div class="container d-flex flex-column align-items-center hidden-text">
  <h2 class="text-center fw-bold"> BRAVO ! 🥳</h2>
  <p class="text-center fw-bold"> Vous avez terminé votre vote, partagez le lien suivant à vos amis
  pour qu’ils puissent voter à leur tour :
  <br>
  <div data-controller="clip-board" data-clip-board-feedback-text-value="Copied!">
  <input style="opacity: 0; height: 0; visibility: hidden; width:0; position: absolute;" class="d-some" data-clip-board-target="input" type="text" value=" <%= deck_url(@deck) %>" readonly>
  <button class="btn btn-primary" data-action="click->clip-board#copy">Copier le lien</button>
</div>
   </p>

<% end %>
